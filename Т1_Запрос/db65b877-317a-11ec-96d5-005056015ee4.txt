{
 'Реквизиты': {
  'Наименование': 'Обмен Canary',
  'ТекстОбработки': 'ОтборУзелАктивен = Новый Структура("УзелАктивен", Истина);
Выборка = ПланыОбмена.Т1_ОбменCanary.Выбрать(ОтборУзелАктивен);
Пока Выборка.Следующий() Цикл
	Если НЕ Выборка.ЭтотУзел Тогда
	
		ЗаписьXML = Новый ЗаписьXML;
		ИмяВрем = ПолучитьИмяВременногоФайла();
		ЗаписьXML.ОткрытьФайл(ИмяВрем);
	
		ПланыОбмена.Т1_ОбменCanary.ВыгрузитьИзмененияДанныхДляУзла(Выборка.Ссылка, ЗаписьXML);
	КонецЕсли;
КонецЦикла;',
  'Комментарий': ''
 },
 'ТЧ': [
  {
   'Переменные': []
  }
 ]
}
